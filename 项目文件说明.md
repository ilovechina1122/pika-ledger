# 项目文件说明（写给非程序员看）

> 多宝记账的项目结构讲解。把程序想象成一栋房子，这份文档就是"户型图 + 每个房间干什么用的说明书"。

---

## 一、整体结构（一栋房子 🏠）

```
D:\pika-ledger\
├─ src\                  ← 房子本体（源代码都在这里）
│  ├─ main\              ← 「后台管家区」：管数据库、管窗口、管通信（你永远看不见它工作）
│  ├─ preload\           ← 「门禁走廊」：界面和管家之间的唯一通道
│  ├─ shared\            ← 「合同区」：管家和界面约定好的规矩（数据长什么样、怎么传）
│  └─ renderer\          ← 「前台装修区」：你眼睛看到的一切界面
├─ build\                ← APP 图标（黄色小狗）
├─ dist\                 ← 打包出来的安装包（成品仓库）
├─ scripts\              ← 各种自动化小工具（生成图标、验证数据、重启开发环境）
└─ 根目录的各种配置文件    ← 「房屋手续文件」（下面逐个讲）
```

**关键比喻**：程序分"前台"和"后台"两部分。
- **前台（renderer）**：负责好看——按钮、颜色、图表
- **后台（main）**：负责靠谱——存数据、查数据，绝不让界面直接碰数据库（安全设计）

---

## 二、根目录文件（房屋手续）

| 文件 | 作用 | 比喻 |
|---|---|---|
| `package.json` | 项目身份证 + 购物清单（列明了要用哪些现成的"零件库"，以及各种命令按钮） | 装修合同 |
| `package-lock.json` | 购物清单的精确版本记录（保证换台电脑装出来的东西一模一样） | 合同附件 |
| `tsconfig.json` | 代码质检标准（TypeScript 会按这个标准检查代码错误） | 质检手册 |
| `electron.vite.config.ts` | 打包工具的配置（把代码变成能运行的程序） | 装修施工方案 |
| `electron-builder.yml` | 安装包制作配置（生成 setup.exe 的规则：名字、图标、安装语言等） | 出厂说明书 |
| `.npmrc` | 下载零件的国内加速通道设置 | 快递地址 |
| `.gitignore` | 告诉 git「哪些东西不用备份」（如 500MB 的零件库、成品安装包） | 搬家清单（不带走的） |
| `claude.md` | 产品文档 + 协作规则（**最重要的文件**，未来每次开发我都会先读它） | 家规 |
| `README.md` | 用户手册（GitHub 仓库首页展示的内容） | 使用说明书 |
| `项目文件说明.md` | 就是你现在看的这份 | 户型图 |

---

## 三、src\shared —— 合同区（规矩）

| 文件 | 作用 |
|---|---|
| `types.ts` | 定义"数据长什么样"：一笔账（金额、类型、分类、日期、备注）、一个分类（名字、emoji、层级）、统计结果等 |
| `ipc.ts` | 定义"前后台怎么通信"：通道名 + 每个操作传什么、返回什么。前后台都必须按这份合同来，改一处漏一处马上会报错（这就是类型检查的威力） |

---

## 四、src\main —— 后台管家区（数据安全的核心）

| 文件 | 作用 |
|---|---|
| `index.ts` | 程序入口：设置数据保存位置、防止同时开两个窗口、创建窗口、退出时关数据库 |
| `db\connection.ts` | 打开/关闭数据库连接（全程序只有一个连接，避免混乱） |
| `db\schema.ts` | 建表图纸：categories 表（分类）、transactions 表（账目），带版本号，以后升级结构不丢数据 |
| `db\seed.ts` | 内置分类清单（16 个一级 + 53 个二级，带 emoji）。**想改默认分类就改这里** |
| `db\backup.ts` | 每日自动备份（保留最近 30 份，防数据丢失的兜底网） |
| `db\categories.repo.ts` | 分类的增删改查：重名校验、软删除（删除不伤历史账单）、恢复 |
| `db\transactions.repo.ts` | 账目的增删改查：金额按"分"存储（杜绝一分钱误差）、分类校验 |
| `db\stats.repo.ts` | 统计计算：月度总览、分类构成（含下钻）、每日收支、月度趋势 |
| `ipc\helpers.ts` | 通信的公共小工具（统一的结果包装：成功/失败） |
| `ipc\index.ts` | 把所有"服务窗口"注册起来 |
| `ipc\categories.ts` | 分类相关的服务窗口（含输入校验：拦下不合法请求） |
| `ipc\records.ts` | 账目相关的服务窗口 |
| `ipc\stats.ts` | 统计相关的服务窗口 |
| `ipc\app.ts` | 应用信息服务（版本号、数据文件夹路径、打开文件夹） |

---

## 五、src\preload —— 门禁走廊

| 文件 | 作用 |
|---|---|
| `index.ts` | 界面唯一能用的通道 `window.api`：前台只能通过这些"服务窗口"办事，接触不到后台的任何危险能力（安全设计） |

---

## 六、src\renderer —— 前台装修区（你看到的界面）

| 文件/文件夹 | 作用 |
|---|---|
| `index.html` | 界面外壳页面（标题、安全策略） |
| `src\main.ts` | 界面启动入口（装上 Pinia 状态管理、路由、主题样式、Element Plus 零件库） |
| `src\App.vue` | 最外层框架（只负责显示页面路由） |
| `src\window.d.ts` | 给 window.api 做类型声明（让质检知道 api 长什么样） |
| `src\router\index.ts` | 页面地图：记一笔 / 账单 / 统计 / 分类管理 四个页面怎么跳转 |
| `src\stores\categories.ts` | 分类数据仓库：全程序共用的分类列表（改分类后各页面同步更新） |
| `src\api\index.ts` | 前后台通信的解包器（把后台返回的"信封"拆开，失败就报错提示） |
| `src\utils\money.ts` | 金额显示格式（分 → "1,234.56"，带 ¥ 符号） |
| `src\utils\date.ts` | 日期显示格式（今天/昨天/2026年09月01日 星期三） |
| `src\styles\theme.css` | **主题色板**：暖黄米白配色、圆角、通用按钮样式。**想换配色就改这里** |
| `src\charts\setup.ts` | 图表引擎的零件注册（只装用得到的，省体积） |
| `src\charts\palette.ts` | 图表专用配色（经过专业校验的 8 色 + 支出红/收入绿） |
| `src\layouts\AppLayout.vue` | 左侧导航栏 + 内容区的大布局 |
| `src\views\AddRecordView.vue` | 「记一笔」页面：金额输入、类型切换、分类选择、日期、备注、保存 |
| `src\views\RecordsView.vue` | 「账单」页面：日期分组、筛选栏、编辑/删除、分组合计 |
| `src\views\StatsView.vue` | 「统计」页面：月份切换、三卡片、四张图表、排行 |
| `src\views\CategoriesView.vue` | 「分类管理」页面：增删改、恢复已删除、笔数统计、数据文件夹入口 |
| `src\components\AmountInput.vue` | 金额输入框（只允许数字和两位小数、回车保存） |
| `src\components\TypeToggle.vue` | 支出/收入切换开关 |
| `src\components\CategoryPicker.vue` | 两级分类选择器（先大类后小类，带返回） |
| `src\components\FilterBar.vue` | 账单筛选栏（类型/日期范围/分类/关键词） |
| `src\components\RecordRow.vue` | 账单里的一行记录（emoji + 名称 + 备注 + 金额 + 操作按钮） |
| `src\components\DateGroupHeader.vue` | 账单分组标题（今天/昨天 + 当日小计） |
| `src\components\RecordFormDialog.vue` | 编辑账目的弹窗 |
| `src\components\EmptyState.vue` | 空状态插画（"还没有数据哦"） |
| `src\components\MonthSwitcher.vue` | 统计页的月份切换器（‹ 2026年9月 ›） |
| `src\components\StatCard.vue` | 收入/支出/结余大数字卡片 |
| `src\components\CategoryPie.vue` | 分类饼图（点击扇区可下钻） |
| `src\components\DailyBarChart.vue` | 每日收支柱状图 |
| `src\components\MonthlyTrendChart.vue` | 近 12 个月趋势折线图 |
| `src\components\RankingList.vue` | 分类排行列表（进度条样式） |
| `src\components\CategoryFormDialog.vue` | 新增/重命名分类的弹窗（含 emoji 选择） |

---

## 七、scripts —— 自动化小工具

| 文件 | 作用 |
|---|---|
| `dev-restart.ps1` | 一键清理开发环境残留进程（开发时用的） |
| `make-icon.js` | 生成 APP 图标（在离屏窗口里画狗狗 SVG 再截图） |
| `verify-stats.js` | 统计数字自动化验证（插入已知金额 → 核对每个统计结果） |

---

## 八、常用命令（在项目根目录的终端里输入）

| 命令 | 作用 |
|---|---|
| `npm run dev` | 启动开发版（窗口 + 改代码自动刷新） |
| `npm run build` | 代码质检 + 编译（发版前必跑） |
| `npm run build:win` | 生成 Windows 安装包到 dist\ 文件夹 |
| `npm run rebuild` | 升级 Electron 或数据库组件后重新适配 |
| `git push` | 把最新代码同步到 GitHub |

---

## 九、想改东西去哪里找？（快速索引）

| 想改什么 | 去哪个文件 |
|---|---|
| 界面配色/圆角/字体 | `src\renderer\src\styles\theme.css` |
| 内置默认分类 | `src\main\db\seed.ts` |
| 记一笔页面布局 | `src\renderer\src\views\AddRecordView.vue` |
| 账单列表样式 | `src\renderer\src\views\RecordsView.vue` |
| 统计图表样式 | `src\renderer\src\views\StatsView.vue` + `components\*.Chart.vue` |
| 分类管理逻辑 | `src\renderer\src\views\CategoriesView.vue` |
| 数据库表结构 | `src\main\db\schema.ts` |
| 统计计算规则 | `src\main\db\stats.repo.ts` |
| APP 图标 | `build\icon.png`（改完重新跑 `npm run build:win`） |
| 安装包配置（名字/语言） | `electron-builder.yml` |

---

*这份文档随项目更新，改了大结构记得让 Claude 同步更新它。*
